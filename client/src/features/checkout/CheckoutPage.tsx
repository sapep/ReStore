import { Typography } from "@mui/material";

function CheckoutPage () {
  return (
    <Typography variant="h3">
      Only logged in users should be able to to see this!
    </Typography>
  );
}

export default CheckoutPage;